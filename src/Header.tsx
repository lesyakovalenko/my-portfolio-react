import React from 'react';
import {Container, Navbar} from "react-bootstrap";

export const Header = ()=>{
    return (
        <Container>
            <Navbar>Navbar</Navbar>
        </Container>
    )
}